import React from 'react';
import { connect } from 'react-redux'

const Login = () => (
  <div>
    <h1> Log up to get swiping! </h1>
    <button>Sign in through facebook</button>
  </div>
);

export default Login;
